export interface IRole{
    //ei interface ta ekhn amadr role component er moddhe use korte hbe coz we know our data is the array of object
    roleId:number,
    role:string

}

export interface Apireponse{
    message:string;
    result:boolean;
    data:any;
}

export interface IDesignation{
    //ei interface ta ekhn amadr role component er moddhe use korte hbe coz we know our data is the array of object
    designationId:number,
    designation:string

}